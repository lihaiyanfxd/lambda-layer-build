from exceptions.business_error import BusinessError
from models.user_model import User


class UserService:

    def get_user(self, user_id: int):

        if user_id == 0:
            raise BusinessError("user_id cannot be 0")

        return User(user_id)
