route() end没有出力
router.py
from app.responses import success_response, error_response
from app.users import get_user
from app.exceptions import BusinessException
import logging

logger = logging.getLogger(__name__)

def route(event):
    logger.info("route() start")

    method = event["httpMethod"]
    path = event["path"]

    logger.info(f"method={method}")
    logger.info(f"method={path}")

    if method == "GET" and path == "/kk":

        query = event.get("queryStringParameters") or {}

        data = get_user(user_id)
        logger.info("route() end")
        return data

    return {
        "statusCode": 404,
        "body": "not found"
    }

user.py
from app.user_service import get_user_service

def get_user(user_id: int):
        user = get_uer_service(user_id)
        return {
                "statusCode":200,
                "body":user
        }
user_service.py
from app.exceptions import BusinessException

def get_user_service(user_id: int):
    if user_id == 0:
        raise BusinessException("no 0")
    return {
        "user_id": user_id,
        "name":"test1"
    }
