- name: Build layer inside AWS Lambda Python 3.10 image
  run: |
    docker run --rm \
      --entrypoint /bin/bash \
      -v "$PWD":/var/task \
      public.ecr.aws/lambda/python:3.10 \
      -c "
        mkdir -p python &&
        pip install fastapi mangum -t python &&
        zip -r fastapi_layer.zip python
      "
